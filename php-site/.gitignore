# Large files
assets/logo.png

# Sensitive config — NEVER commit real credentials
config.php

# Razorpay keys and any credential files in data/
data/karthik-rzp-key.csv
data/*.csv
data/*.key
data/*.env

# Local data (runtime files)
data/orders.json

# OS files
.DS_Store
Thumbs.db
